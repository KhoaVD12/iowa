apiVersion: v2
name: iowa
description: A Helm chart for Iowa application with SQL Server and Cassandra
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  # Cassandra - Official Bitnami chart for subscriptions data
  - name: cassandra
    version: 12.3.11
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: cassandra.enabled
